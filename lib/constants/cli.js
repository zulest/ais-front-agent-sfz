/** User-facing CLI and chat triggers (single source of truth). */
/** Acento del CLI: banner, éxitos, prompts (celeste). */
export const CLI_ACCENT_HEX = '#5eb8e8';

export const NPM_PACKAGE = 'ais-agente-front-winforms';
/** Comando principal del paquete cliente (evita colisión con el orquestador de backend). */
export const SLASH_TRIGGER = '/sfz-front';
export const WORD_TRIGGER = 'sfz-front';
/** Short npx invocation (requires bin name `sfz-front` in package.json). */
export const NPX_SHORT = 'npx sfz-front';
/** Full package name invocation. */
export const NPX_LONG = 'npx ais-agente-front-winforms';
